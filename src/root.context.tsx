import React from 'react';
import { RootContextType } from './types';

// Creating context for root values
const RootContext = React.createContext({} as RootContextType);

export default RootContext;

